const HOME = "/";
const GALLERY = "/gallery";
const NOTICE = "/notice";
const UPLOAD = "/upload";

const routes = {
  home: HOME,
  gallery: GALLERY,
  notice: NOTICE,
  upload: UPLOAD,
};

export default routes;
